---
summary: "Rename: bls-mcp-server → bls-labor-mcp-server. npm, Docker, and MCP registry identifiers updated. Tool names (bls_*) unchanged."
breaking: true
security: false
---

# 0.2.0 — 2026-05-24

Package rename — no functional changes. Tool names stay `bls_*`.

## Changed

- **GitHub repo** renamed from `cyanheads/bls-mcp-server` to `cyanheads/bls-labor-mcp-server`. Old URLs redirect automatically.
- **npm package** renamed from `@cyanheads/bls-mcp-server` to `@cyanheads/bls-labor-mcp-server`. Old package deprecated with a pointer to the new name.
- **Docker image** renamed from `ghcr.io/cyanheads/bls-mcp-server` to `ghcr.io/cyanheads/bls-labor-mcp-server`.
- **Hosted endpoint** moved to `https://bls-labor.caseyjhand.com/mcp` (subdomain change handled separately; see [#21](https://github.com/cyanheads/bls-labor-mcp-server/issues/21)).
- **MCP registry** identifier updated to `io.github.cyanheads/bls-labor-mcp-server`.
- **`server.json`** — `name`, `repository.url`, `remotes[].url`, `packages[].identifier` updated.
- **`manifest.json`** — `name` updated to `bls-labor-mcp-server`.
- **`package.json`** — `name`, `bin`, `bundle` script, `mcpName`, `repository`, `homepage`, `bugs`, `author` URL updated.
- **`Dockerfile`** — OCI `title` label, `source` label, log directory path, `LOGS_DIR` env var updated.
- **`src/index.ts`** — JSDoc fileoverview and catalog error prefix updated.
- **Issue templates** — description strings updated.
- **Docs** (`design.md`, `idea.md`, `tree.md`) — title and server name references updated.

## Removed

- **`@cyanheads/bls-mcp-server`** npm package deprecated; `bun add @cyanheads/bls-mcp-server` installs show a deprecation notice pointing to `@cyanheads/bls-labor-mcp-server`.
